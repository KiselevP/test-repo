{% block paragraph %}
        {% block content %}
                <div class="service-item">
                        <div>{{ content.field_img|without('#theme') }}</div>
                        {{ content.field_headline_div|without('#theme') }}
                        <div>{{ content.field_description|without('#theme') }}</div>
                </div>
        {% endblock %}
{% endblock paragraph %}